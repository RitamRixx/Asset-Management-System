"""
FastAPI routers package, one module per resource (auth, employees, assets,
assignments, repairs, reports, ...) per section 41's API structure. Only
`health.py` exists in Phase 1; the rest are added phase by phase.
"""
